package jelena.milivojevic;

public class ParnaSuma {

	public static void main(String[] args) {
		/*
		 * Napisati program kojim se ispisuju susedni članovi niza X od N elemenata čija
		 * je suma parna.
		 * 
		 * Objašnjenje:
		 * 
		 * Provera se izvršava naredbom if( (x[i - 1] + x[i]) % 2 == 0) u kojoj se
		 * izvodi sabiranje dva susedna člana niza x[i - 1] + x[i] a naredbom % 2 == 0
		 * se proverava parnost sume.
		 */
	}

}
